# Flask API Service Starter

This is a minimal Flask API service starter.

## Getting Started

Server should run automatically when starting a workspace. To run manually, run:
```sh
./devserver.sh
```